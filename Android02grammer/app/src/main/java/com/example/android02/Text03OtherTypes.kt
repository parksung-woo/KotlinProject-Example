package com.example.android02

fun main() {
    println("boolean")

    println("true : ${true}")
    println("false : ${false}")
    println("!true : ${!true}")
    println("!false : ${!false}")

    //Unit :java void
    //Noting : exception 발생타입(비정상종료시 사용)
    //Any >> 최상위 타입(java:Object)
    //해당Type? >> 물음표가 붙으면 null을 허용하겠다.
    //ex) Any?, Int? >> null허용 / Any, Int >> null불가
}